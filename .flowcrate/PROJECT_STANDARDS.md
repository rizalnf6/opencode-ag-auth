# Project Standards

> Auto-generated by `flowcrate standards init`.
> Content is project-specific based on detected configuration.

---

## Tech Stack

| Category | Technology |
|----------|------------|
| **Language** | Typescript |
| **Frameworks** | typescript |
| **Package Manager** | pnpm |
| **Testing** | Vitest |
| **Node Version** | >=20.0.0 |

## Commands

> [!IMPORTANT]
> Use these exact commands. Do not guess or infer alternatives.

| Command | Description |
|---------|-------------|
| `pnpm dev` | Start development server |
| `pnpm build` | Production build |
| `pnpm build` | UI-only build |
| `pnpm test` | Run tests |
| `pnpm typecheck` | Type checking |
| `pnpm lint` | Code linting |
| `pnpm clean` | Clean build artifacts |
| `pnpm test:watch` | Watch mode |
| `pnpm test:e2e:regression` | End-to-end tests |
| `pnpm test:e2e:models` | End-to-end tests |
| `pnpm test:coverage` | Test coverage |

## Build & Release

### Development Workflow

1. **Development**: `pnpm dev`
3. **Type Checking**: `pnpm typecheck` (before commits)

### Build Workflow



## Project Structure

```
opencode-ag-auth/
├── src/                 # Source code
├── dist/                # Build output
└── scripts/             # Build/utility scripts
├── package.json
├── tsconfig.json
```


## Environment Variables

*(No .env files detected)*

## Ports & URLs

*(No ports detected - check framework documentation)*

## Naming Conventions

### General
- **Variables/Functions**: `camelCase`
- **Components**: `PascalCase`
- **Constants**: `SCREAMING_CASE`
- **Files**: `camelCase`
- **Directories**: `kebab-case`


### Examples
```typescript
// Variables and functions
const userName = "example";
const fetchUserData = () => {};

// Components
const UserProfile = () => {};

// Interfaces and Types
interface UserData {}
type UserStatus = "active" | "inactive";

// Constants
const MAX_RETRY_COUNT = 3;
```




## Code Architecture

> [!IMPORTANT]
> AI agents MUST check these locations when working on specific features.

### Key Code Locations

| Feature | Location | Purpose |
|---------|----------|---------|
| *(auto-detect)* | *(scan project)* | *(determined on init)* |

### Frontend Component Structure

1. Create new component in `src/components/<ComponentName>/`
2. Export from `src/components/index.ts`
3. Use named exports for better tree-shaking

## AI Agent Guidelines

### Critical Rules

1. **ALWAYS use pnpm** - Never use other package managers
2. **Type Safety** - Maintain strict TypeScript types, run typecheck before commits
3. **Clean Builds** - Run `pnpm clean` or equivalent when dependency issues occur

### Code Style

- **Components**: Functional components with JSX
- **Imports**: Use path aliases, organize imports (external → internal → relative)
- **Security**: Sanitize user input, validate on both client and server

### Testing & Validation

- **Type Checking**: `pnpm typecheck`
- **Unit Tests**: `pnpm test`



## Security Considerations

- **Input Sanitization**: Always validate and sanitize user-generated content


## Performance

- **Bundle Size**: Be mindful of dependencies, check bundle analyzer
- **Lazy Loading**: Use dynamic imports for large components
- **Tree Shaking**: Ensure unused exports are eliminated


---

*Generated: 2026-02-15 02:42:14 UTC*
*Re-run `flowcrate standards init` with AI available for comprehensive generation.*
